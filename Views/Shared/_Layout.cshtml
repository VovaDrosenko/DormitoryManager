<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - DormitoryManager</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/DormitoryManager.styles.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                @if (User.Identity.IsAuthenticated)
                {
                    <a class="navbar-brand" asp-controller="Dashboard" asp-action="Index">DormitoryManager</a>
                }
                else
                {
                    <a class="navbar-brand" asp-controller="Home" asp-action="Index">DormitoryManager</a>
                }
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-end">
                    <ul class="navbar-nav">
                        <li class="nav-item btn btn-outline-success">
                            <a class="nav-link" asp-area="" asp-controller="Dashboard" asp-action="Signin">Sign In</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-3">
                <div id="sidebar">
                    <ul>
                        <li class="nav-item">
                            <a id="index-link" class="nav-link text-dark @((ViewContext.RouteData.Values["Action"] as string == "Form") ? "active" : "")"
                               asp-area="" asp-controller="Home" asp-action="Form">Форма поселення</a>
                        </li>
                        <li class="nav-item">
                            <a id="index-link" class="nav-link text-dark @((ViewContext.RouteData.Values["Action"] as string == "Documents") ? "active" : "")"
                               asp-area="" asp-controller="Home" asp-action="Documents">Список документів</a>
                        </li>
                    </ul>
                </div>
            </div>
            <div class="col-md-9">
                <main role="main" class="pb-3">
                    @RenderBody()
                </main>
            </div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
